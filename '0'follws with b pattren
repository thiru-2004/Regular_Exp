#Write a Python program that matches a string that has an a followed by zero or more b's
import re
txt=input("enter the text")
if re.search('^0+b',txt):
   print("string is accpected")
else:
   print("string is not accpected")

